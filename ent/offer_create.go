// Code generated by ent, DO NOT EDIT.

package ent

import (
	"MyBank-Go-Backend/ent/bank"
	"MyBank-Go-Backend/ent/offer"
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// OfferCreate is the builder for creating a Offer entity.
type OfferCreate struct {
	config
	mutation *OfferMutation
	hooks    []Hook
}

// SetType sets the "type" field.
func (oc *OfferCreate) SetType(s string) *OfferCreate {
	oc.mutation.SetType(s)
	return oc
}

// SetDescription sets the "description" field.
func (oc *OfferCreate) SetDescription(s string) *OfferCreate {
	oc.mutation.SetDescription(s)
	return oc
}

// SetLink sets the "link" field.
func (oc *OfferCreate) SetLink(s string) *OfferCreate {
	oc.mutation.SetLink(s)
	return oc
}

// SetBankID sets the "bank" edge to the Bank entity by ID.
func (oc *OfferCreate) SetBankID(id int) *OfferCreate {
	oc.mutation.SetBankID(id)
	return oc
}

// SetBank sets the "bank" edge to the Bank entity.
func (oc *OfferCreate) SetBank(b *Bank) *OfferCreate {
	return oc.SetBankID(b.ID)
}

// Mutation returns the OfferMutation object of the builder.
func (oc *OfferCreate) Mutation() *OfferMutation {
	return oc.mutation
}

// Save creates the Offer in the database.
func (oc *OfferCreate) Save(ctx context.Context) (*Offer, error) {
	return withHooks(ctx, oc.sqlSave, oc.mutation, oc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (oc *OfferCreate) SaveX(ctx context.Context) *Offer {
	v, err := oc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (oc *OfferCreate) Exec(ctx context.Context) error {
	_, err := oc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (oc *OfferCreate) ExecX(ctx context.Context) {
	if err := oc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (oc *OfferCreate) check() error {
	if _, ok := oc.mutation.GetType(); !ok {
		return &ValidationError{Name: "type", err: errors.New(`ent: missing required field "Offer.type"`)}
	}
	if _, ok := oc.mutation.Description(); !ok {
		return &ValidationError{Name: "description", err: errors.New(`ent: missing required field "Offer.description"`)}
	}
	if _, ok := oc.mutation.Link(); !ok {
		return &ValidationError{Name: "link", err: errors.New(`ent: missing required field "Offer.link"`)}
	}
	if len(oc.mutation.BankIDs()) == 0 {
		return &ValidationError{Name: "bank", err: errors.New(`ent: missing required edge "Offer.bank"`)}
	}
	return nil
}

func (oc *OfferCreate) sqlSave(ctx context.Context) (*Offer, error) {
	if err := oc.check(); err != nil {
		return nil, err
	}
	_node, _spec := oc.createSpec()
	if err := sqlgraph.CreateNode(ctx, oc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	oc.mutation.id = &_node.ID
	oc.mutation.done = true
	return _node, nil
}

func (oc *OfferCreate) createSpec() (*Offer, *sqlgraph.CreateSpec) {
	var (
		_node = &Offer{config: oc.config}
		_spec = sqlgraph.NewCreateSpec(offer.Table, sqlgraph.NewFieldSpec(offer.FieldID, field.TypeInt))
	)
	if value, ok := oc.mutation.GetType(); ok {
		_spec.SetField(offer.FieldType, field.TypeString, value)
		_node.Type = value
	}
	if value, ok := oc.mutation.Description(); ok {
		_spec.SetField(offer.FieldDescription, field.TypeString, value)
		_node.Description = value
	}
	if value, ok := oc.mutation.Link(); ok {
		_spec.SetField(offer.FieldLink, field.TypeString, value)
		_node.Link = value
	}
	if nodes := oc.mutation.BankIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   offer.BankTable,
			Columns: []string{offer.BankColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(bank.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.bank_offers = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// OfferCreateBulk is the builder for creating many Offer entities in bulk.
type OfferCreateBulk struct {
	config
	err      error
	builders []*OfferCreate
}

// Save creates the Offer entities in the database.
func (ocb *OfferCreateBulk) Save(ctx context.Context) ([]*Offer, error) {
	if ocb.err != nil {
		return nil, ocb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(ocb.builders))
	nodes := make([]*Offer, len(ocb.builders))
	mutators := make([]Mutator, len(ocb.builders))
	for i := range ocb.builders {
		func(i int, root context.Context) {
			builder := ocb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*OfferMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ocb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ocb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ocb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (ocb *OfferCreateBulk) SaveX(ctx context.Context) []*Offer {
	v, err := ocb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ocb *OfferCreateBulk) Exec(ctx context.Context) error {
	_, err := ocb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ocb *OfferCreateBulk) ExecX(ctx context.Context) {
	if err := ocb.Exec(ctx); err != nil {
		panic(err)
	}
}
